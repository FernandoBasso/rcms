<%= form_for([:admin, @post_category], html: { class: 'admin-form' }) do |f| %>
  <% if @post_category.errors.any? %>
    <div id='error_explanation'>
      <h2><%= pluralize(@post_category.errors.count, 'error') %> prohibited this post_category from being saved:</h2>

      <ul>
      <% @post_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class='form-group row'>
    <div class='col-md-12'>
      <%= f.label :title, { class: 'form-control-label' } %>
      <%= f.text_field :title, { class: 'form-control' } %>
    </div>
  </div>
  <div class='form-group row'>
    <div class='col-md-12'>
      <%= f.label :description, { class: 'form-control-label' } %>
      <%= f.text_field :description, { class: 'form-control' } %>
    </div>
  </div>
  <div class='form-group row'>
    <div class='col-md-12'>
      <%= f.label :slug, { class: 'form-control-label' } %>
      <%= f.text_field :slug, { class: 'form-control' } %>
    </div>
  </div>
  <div class='actions'>
    <%= f.submit 'Gravar', { class: 'btn btn-primary' } %>
  </div>
<% end %>
