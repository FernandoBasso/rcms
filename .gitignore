# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# If you find yourself ignoring temporary files generated by your text editor
# or operating system, you probably want to add a global ignore instead:
#   git config --global core.excludesfile '~/.gitignore_global'

# Ignore bundler config.
/.bundle

# My Beloved `tags` file can't go here. Sorry tags.
tags

# .env used only locally but should not commit sensitive information anyway.
.env

# Ignore all logfiles and tempfiles.
/log/*
!/log/.keep
/tmp
config/database.yml

# We are using bootstrap 4 alpha, let's ignore it this way for now.
app/assets/stylesheets/bootstrap/
